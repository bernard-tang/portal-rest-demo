import dashboard from './dashboard';
import pages from './pages';
import products from './products';
import other from './other';
import users from './users';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, users, products, other]
};

export default menuItems;
